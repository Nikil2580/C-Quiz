#include <stdio.h>
#include <stdlib.h>

int main()
{
    printf("%d\n", rand() % 1000);   // Ans: 41 
    return 0;                        // Always the first random no will 41 because it is a pseudo random number
}
